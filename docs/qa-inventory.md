# Verification inventory

Version 0.1.0 is uncalibrated research software. Passing these checks verifies specified software behavior, not biological truth.

## Numerical and scientific checks

The automated suite covers zero-primary/secondary/target/release/internalization controls, untreated receptor steady state, nonnegativity tolerance, primary and secondary amount conservation, cell-state conservation, all four addition orders, exact zero-delay equivalence, endpoint-only delayed dosing, reporter accumulation, reporter-loss separation from killing, monotonic saturation, input validation, solver tolerance, accelerated/reference RHS parity, the analytic equilibrium maximum, and the high-primary asymptote.

`tools/verify_atlas.py` checks every atlas row's dimensions and numeric validity and independently recomputes nine representative dose conditions from the Python model. Additional mechanism-lab consistency is checked by the same script. The complete data remain synthetic and uncalibrated.

`results/sensitivity.json` retains both favorable and unfavorable spatial-resolution tests. The transport stress case is explicitly not mesh-converged. Parameter uncertainty is explored through discrete scenarios, not statistical confidence intervals or an inferred posterior.

## Browser checks

The public explorer should load without installation; expose both atlas and mechanism-lab modes; update all selectors and paired plots; distinguish molecule-count readouts from normalized responses; export usable CSV, parameter JSON, and chart PNG; expose exact-value tables; handle data-loading errors; support light/dark mode and keyboard focus; and fit desktop/mobile widths without horizontal page overflow.

The browser does not solve a second, approximate model. It displays the JSON generated by the Python implementation and labels connection lines as visual guides.

## Publication checks

Before release: run the test suite; recompute reference rows; inspect figures; test browser downloads and state transitions; validate citation metadata and the MIT license; screen public text for private context; confirm Pages resolves; and check the remote CI run. Record any check that cannot be completed rather than implying it passed.

## Local verification record, September 18, 2026

All 34 automated tests passed. All atlas and mechanism-lab rows passed structure and finite-value checks; nine atlas conditions and three mechanism-lab conditions were independently recomputed successfully.

Playwright checks exercised both explorer modes, all seven mechanism cases, all addition orders, receptor and trafficking selectors, observation times, alternate readouts, dose inspection, exact-value tables, theme switching, reset, and CSV/JSON/PNG downloads. No JavaScript errors were observed. The CSV contained the expected 117 data rows, and parameter JSON preserved the selected scenario. Desktop and mobile explorer layouts were inspected. The mathematical page rendered 198 MathJax containers with no MathJax errors and no horizontal page overflow at 390 px after the inline-equation layout correction. Both article figures loaded.

The notebook's code cells were syntax-checked, but an authenticated hosted Colab session was not executed. Its scientific implementation is the same tested Python package. Hosted runtime availability and third-party dependencies remain external requirements. Remote CI and Pages status can be inspected in the repository; local checks alone do not establish their success.

## Worked-example addition: QA scope

Check the “Follow one simulation” navigation and direct hash link; all three dose conditions at all six time points; the initial-state zero response; the 72 h endpoint comparison; payload delivery/loss arithmetic; independence from atlas controls; all four downloads; the static worked-equation page; light/dark themes; and desktop/mobile overflow. Off-happy-path checks must cover a missing or invalid trace data file and JavaScript-disabled access to the static walkthrough. Recompute all three trajectories from Python in the automated tests and compare matching endpoints against the already published mechanism lab. This addition changes the viewer and documentation, not the v0.1.0 numerical model.

The requested publication theme is dark by default on the explorer, equations, article, and worked example, even when the operating system prefers light mode. The manual light/dark controls remain available. The cover must use actual viewer captures, retain visible synthetic/uncalibrated labeling, match the dark series presentation, and avoid implying that the computations are experimental data.

## Worked-example and literature update verification, September 18, 2026

All 39 tests passed, including recomputation of all three worked trajectories, download consistency, payload bookkeeping, and matching endpoints against the original mechanism lab. The separate atlas verifier passed all structure/finite-value checks and independently recomputed nine atlas and three mechanism-lab conditions.

Playwright exercised all 18 worked-example snapshots, all four downloadable files, control independence, the seven-row endpoint comparison, dark default under a light operating-system preference, manual theme switching, and 375 px mobile layout. Missing data, invalid data schema, and JavaScript-disabled access were tested. The equations, article, and static worked example opened in dark mode without mobile page overflow or MathJax errors. No JavaScript errors were observed.

The expanded article rendered all eleven bibliography entries and all three images. Desktop and mobile article/reference screenshots were visually inspected. The two cover images use actual reference-viewer captures and explicitly identify the results as synthetic and uncalibrated; the composition script and source captures are committed. The numerical model and v0.1.0 release tag remain unchanged.

## Editorial mathematics revision

The article integrates equilibrium assembly, productive payload balance, and the fluorescence history integral into continuous prose. Verify that all introduced symbols and limiting assumptions are defined, the integral is consistent with the implemented reporter ODE, and the existing synthetic numerical example is unchanged. Browser checks cover rendered mathematics without errors, equation fit at desktop and 375 px, dark default, the full light/dark toggle cycle, three figure loads, eleven references, working navigation, and absence of the article-only software helper. Off-path checks cover blocked MathJax (readable TeX fallback) and a narrow viewport. Numerical model tests remain a regression gate; no model parameters or simulated datasets change in this revision.

All 41 tests passed. Two new regression cases reconstruct fluorescence from the article's history integral at 12, 24, 48, and 72 hours, with reporter loss disabled and enabled; each matches the ODE observation within an absolute tolerance of 0.000002. Playwright verified all four mathematical displays without rendering errors or internal horizontal scrolling at 1440 px and 375 px. The article defaults to dark under a light system preference, its theme toggle completes a full cycle, all three figures load, and all eleven references render. The walkthrough navigation and blocked-MathJax TeX fallback passed. Desktop and mobile screenshots were visually reviewed; no clipping, broken images, or page overflow was found. The article-only software helper and platform-specific source filename were removed, while scientific provenance and synthetic-data disclosures were retained.

The public article was verified after deployment with all four displays, eleven references, dark mode, and no MathJax errors. GitHub model verification passed on Python 3.10 and 3.12 for the editorial revision, including the independent atlas checks. The LinkedIn/Substack publication export mechanically preserves the manuscript and citations while substituting portable inline notation and four equation-image positions. All eight light/dark equation PNGs were exported from the rendered article, with the assembly and fluorescence-history images visually inspected. The four existing cover/scientific figure files are copied without modification. No posting to either publication platform is performed.

## Series CCXXIV and introductory framing

Verify the new mechanistic introduction precedes the numerical example, and that the accumulation definition preserves reagent retention without implying constant free concentrations or monotonic reporter behavior. Check the exact series line, “Data-Rich, Insight-Poor — CCXXIV,” on both cover sizes and both manuscript exports. Regenerate the mechanism-map caption from committed simulation data only. Check consistent accumulation labels in the article, explorer, worked example, and mathematical specification; inspect covers for label overlap or clipping. Recheck four rendered mathematical displays, eleven references, figure loads, dark/light controls, and 375 px viewport fit. The numerical equations, parameters, and simulated data must remain unchanged.

All 41 model tests passed after this revision. The two cover sizes and revised mechanism-map image were visually inspected without text clipping or overlap. Playwright confirmed the exact series line, introduction order, explicit accumulation definition, absence of the previous format label in the article, four error-free mathematical displays, eleven references, all figure loads, dark default, and both theme transitions. At 375 px there was no page or displayed-equation overflow; the explorer visibly used the accumulation label after navigation from the article. No JavaScript errors occurred. Both manuscript exports and the publication-kit figures were regenerated from the same source files. The numerical model, parameters, and simulation datasets were not changed.

## Calibration note

The added “Calibration and prediction” section distinguishes synthetic parameter choices from experimental estimation, identifies the exact yield-by-release-efficiency confounding in the implemented delivery term, and separates parameter fitting from independent predictive validation. Check that all three paragraphs appear in the browser article and portable manuscript, that the four displays and eleven references remain intact, and that the note fits desktop and mobile widths. This editorial addition changes no equations, parameters, data, or calibration status.

All 41 tests passed. Both manuscript exports contain the complete note. Browser verification confirmed the note and independent-validation wording, four mathematical displays without MathJax errors, eleven references, and the dark default. Desktop and 375 px screenshots were visually inspected without clipping or page overflow. Git comparison confirmed no changes to the numerical implementation, tests, results, or atlas datasets.
